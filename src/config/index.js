const config = {
  googleApiKey: process.env.GOOGLE_API_KEY,
  api: {
    host: 'https://api.clearedforuse.com',
    lambdaHost: 'https://api-nonlive.everysign.com/main',
  },
};

export default config;
